{% extends 'admin.html.twig' %}

{#{% form_theme form with 'bootstrap_4_layout.html.twig' only %}#}

{% set breadcrumbs = [{text: 'Country'}, {text: 'List all countries'}] %}
{% set quick_actions = [{icon: 'flaticon-list', text: 'List all countries'}, {icon: 'flaticon-add', url: path('country_create'), text: 'Create a country'}] %}

{% block title %}List Country{% endblock %}
{% block subheader_title %}List all countries{% endblock %}
{% block quick_sidebar_content %}
{% endblock %}
{% block content %}
    <div class="m-content">
        <div class="m-portlet m-portlet--full-height m-portlet--tabs m--padding-30">
            <div class="table-responsive-lg m-datatable m-datatable--default">
                <table class="table table-bordered table-hover table-striped">
                    <thead class="thead-dark">
                    <tr>
                        <th scope="col">Name</th>
                        <th scope="col">Code</th>
                        <th scope="col">Continent</th>
                        <th scope="col">Created at</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for item in pager.currentPageResults %}
                        <tr class="row-clickable" data-href="{{ path('country_edit', {id: item.id}) }}" title="{{ path('country_edit', {id: item.id}) }}">
                            <td>{{ item.name }}</td>
                            <td>{{ item.code }}</td>
                            <td>{{ item.continent }}</td>
                            <td>{{ item.createdAt|date('Y-m-d h:i') }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
                {% include 'pager.html.twig' with {pager: pager} %}
            </div>
        </div>
    </div>
{% endblock %}
