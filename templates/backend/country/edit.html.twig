{% extends 'admin.html.twig' %}

{% set breadcrumbs = [{url: path('country_index'), text: 'Country'}, {text: 'Edit'}] %}
{% set quick_actions = [{icon: 'flaticon-list', url: path('country_index'), text: 'List all countries'}, {icon: 'flaticon-add', url: path('country_create'), text: 'Create'}] %}

{% block title %}Edit Country{% endblock %}
{% block subheader_title %}Edit a country{% endblock %}
{% block content %}
    <div class="m-content">
        <div class="m-portlet m-portlet--full-height m-portlet--tabs m--padding-30">
            <div class="row">
                <div class="col-lg-10 col-xl-6">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <div class="form-group row">
                        <div class="col-sm-9 offset-sm-3">
                            <button class="btn btn-primary m-btn m-btn--icon" type="submit">
                                <i class="la la-save"></i>
                                Update
                            </button>
                            <button class="btn btn-outline-primary m-btn m-btn--icon" type="submit"
                                    name="redirect_url" value="{{ path('country_index') }}">
                                <i class="flaticon-list"></i>
                                Update & back to list
                            </button>
                        </div>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
